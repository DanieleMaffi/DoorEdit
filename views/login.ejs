<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Log in</title>
    <link rel="icon" type="images/x-icon" href="src/minilogo.png" />
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <div id="alertBox" class="container"
        style="display: flex; height: 100vh; align-items: center; flex-direction: column; justify-content: center;">
        <div class="card mx-auto form-card topdown-animation">
            <div class="card-body">
                <form action="/auth/login" method="POST">
                    <h4 class="mb-4">Portale d'accesso</h4>
                    <div class="mb-4">
                        <label for="username" class="form-label">Username</label>
                        <input type="text" placeholder="Username" class="form-control" id="username"
                            aria-describedby="usernameHelp" name="username">
                    </div>
                    <div class="mb-4">
                        <label for="password" class="form-label">Password</label>
                        <input type="password" class="form-control" id="password" name="password">
                    </div>
                    <button type="submit" class="btn btn-primary topdownslower-animation">Log in</button>
                    <!--<a style="float: right; position: relative; top: 0.5vh;" class="idLink"
                        href="/auth/forgotPassword">Password dimenticata</a>-->
                </form>
            </div>
        </div>
        <% if (message) { %>
            <div class="alert-container">
                <h5 class="alert alert-danger mt-4 form-card topdown-animation">
                    <%= message %>
                </h5>
            </div>
            <% } %>
    </div>
</body>

</html>